# FirePlay 重构清单：联机前置

> **目的**：在进入实时联机之前，把"输入 → 意图/命令收口 → 领域 POCO 化"这道缝补齐。
> 这一次重构同时解决三件事：
> 1. 解锁"加表情 / 赠与 / 吉他仪式"（当前几乎无法添加）；
> 2. 满足"尽量不写 MonoBehaviour、不在 Update 里轮询"的工程习惯；
> 3. 拿到实时联机的入场券（状态收口 + 领域逻辑可脱离 MonoBehaviour 跑在服务器侧）。
>
> **顺序**：重构 → 实时（服务器权威、锁在中心火堆、零 peer 时单机完整）→ 异步（几乎是实时后端的持久化投影）。
> **不要**在 Step 3 + Step 4 未完成时开始糊 netcode——那会精确撞上"无法重构、无法再开发"。

---

## 诊断结论（为什么是重构而不是重写）

当前代码的**硬骨头已经搭好**，不需要重写：`PlayerModeController` 已存在；`PlayerInteraction`
已有单一已解析目标（`CurrentInteractTargetKind` / `CurrentInteractPrompt`）；仪式扩展点干净
（`RestSpotRitual` + `RestSpot` 遍历回调）；连续添火、衰减、退役痕迹、应急取火均已就位。

问题集中在两点：

1. **"输入 → 意图"没有唯一主人**：`Emote` / `Interact` 被多个仪式各自在 Update 里轮询、自门禁
   （`MarshmallowInteraction.cs:69`、`FishingInteraction.cs:94`）；`PlayerInteraction.Update`
   有两条并行分发路径（已解析目标 `:152-157` vs 移动端动词按钮 `:161-195` + 漂移语义的
   `UpgradeCampfire :197-204`）。
2. **FirePlay 玩法代码完全绕开了项目自带框架**：没有一处用 `GameEventBus`（`Assets/Events`）或
   `GameInstanceSubsystem`（`Assets/Core`），而是 `SerializeField` 直连 + `FindFirstObjectByType`
   + 本地事件 + Update 轮询。**它不是在你的架构里长出来的**——这是"掌控不住 / 感觉陌生"的一大根源。

**本清单的核心，就是让 FirePlay 真正嵌进 `GameEventBus` + `GameInstanceSubsystem`。**

---

## Step 0 · 立四条 house rule（贴进仓库，喂给 AI）

1. **领域逻辑写在纯 C# 类（POCO）里**；MonoBehaviour 只做"读输入 / 驱动表现 / 持有引用"的薄适配层。
2. **不在 Update 里轮询原始输入**；输入走 `InputAction.performed` 回调。Update 只保留每帧连续量
   （相机插值、火势衰减 tick）。
3. **共享状态的每一次变更，只能走唯一的命令执行口**，并通过 `GameEventBus` 抛领域事件；
   任何组件不得就地改别人的状态。
4. **依赖走 `GameInstanceSubsystem`（按接口 `Get/GetOrCreate`），跨系统通信走 `GameEventBus`**；
   禁止新增 `FindFirstObjectByType`。

> 这四条就是判断 AI 产出"收不收"的筛子。

---

## 总线使用边界（先读，避免误用）

### 核心服务注册表

R0 完成时，应能从 `GameInstanceSubsystem` 明确定位以下核心逻辑：

| 接口 | 当前实现 | 未来替换 |
|---|---|---|
| `IEventPublisher` | `GameEventBus` | 保持不变 |
| `IInteractionRouter` | 本地上下文路由 | 网络下仍保留本地路由 |
| `IWorldCommandExecutor` | 单机直接执行 | Host／Server 命令网关 |
| `IWorldObjectRegistry` | Stable ID 场景索引 | 网络生成对象索引 |
| `IPlayerIdentityService` | `local.player` 身份 | NGO ClientId／账号身份映射 |
| `IWorldStateRepository` | 暂不进入 R0 | R3 权威持久化适配器 |

`LocalPlayerContext` 不是全局业务服务：它只标记当前客户端的本地 Player，并持有该 Player 的场景组件引用。

**上 `GameEventBus`（实现 `IGameEvent`）**：语义层——
- **意图**：`EmoteGestureRequested` / `TendCampfireRequested` / `RitualPrimaryRequested` …
- **领域事件**：`CampfireStateChanged` / `FishCaught` / `PlayerModeChanged` …

**不上总线（留本地）**：
- **原始每帧输入**：`Move / Look` 向量、`InputAction.performed` 回调（输入层内部事）；
- **每帧连续量**：相机插值、火势衰减 tick。

**分层**：
```
InputAction.performed（本地回调）
   → Router（本地：按 模式 + 活跃仪式 + 目标 解析）
      → bus.Publish(new TendCampfireRequested(...))       // 语义意图上总线
         → CommandExecutor 订阅并执行（唯一权威变更口）
            → bus.Publish(new CampfireStateChanged(...))   // 领域事件上总线
               → 视觉 / 音效 / UI / 存档 订阅
```

> ⚠️ **先修总线自身的坑**：`GameEventBus.cs:105` 每次 `Publish` 都 `Debug.Log`。
> 路由任何稍频繁的事件前，把这行用 verbose 开关包起来，否则刷爆控制台 + 吃性能。
> ⚠️ MonoBehaviour 订阅方必须在 `OnDisable / OnDestroy` 里 `Unsubscribe`（总线不自动清理）。

**架构验收**：核心服务只能通过上述接口从 `GameInstanceSubsystem` 获取；不得新增静态 Singleton、第二套 Service Locator 或另一个事件总线。

---

## Step 1 · 输入进 Router（keystone，最低风险，先做）

**为什么**：`FirePlayPlayerInput` 现在暴露 `WasPressedThisFrame()` 轮询属性，逼每个消费方在 Update 里查。

**怎么改**：
- 保留 `Move / Look` 连续量属性；
- "按下"类改为在 `InputAction.performed` 回调里**直接通知 Router**（本地方法调用，非总线、非轮询）：
  ```csharp
  _emoteAction.performed += _ => _router.OnRawInput(RawInput.Emote);
  ```
- 在 `OnEnable / OnDisable` 订阅 / 退订，避免泄漏。

**完成标准**：全项目搜不到对 `WasPressedThisFrame()` 的消费；没有任何组件在 Update 里读输入。
**别做**：不动 Input Actions 资产、不改按键绑定；**原始输入不要上总线**。

---

## Step 2 · Router 解析上下文并发布意图（单一主人，解锁表情 / 赠与 / 吉他）

**为什么**：现在 `Emote` 被多处各自抢用，没人统一决定"此刻这个键是什么意思"。

**怎么改**（新建 `InteractionRouter`，注册进 `GameInstanceSubsystem`）：
- 根据 **`当前模式 + 活跃仪式 + 最近交互目标`** 把原始输入翻译成语义意图，`Publish` 到总线：
  ```csharp
  // 意图事件都实现 IGameEvent
  public readonly struct TendCampfireRequested : IGameEvent { public readonly string CampfireId; /* ... */ }
  public readonly struct EmoteGestureRequested : IGameEvent { /* ... */ }
  // Router 内部：
  bus.Publish(new TendCampfireRequested(target.CampfireId));
  ```
- 例：`Emote` → 探索时 = `EmoteGestureRequested`；棉花糖 / 钓鱼仪式中 = `RitualPrimaryRequested`。**一个地方裁决。**
- 现成上下文：`PlayerInteraction.CurrentInteractTargetKind` 直接喂给 Router。

**完成标准**：加"探索时挥手" = Router 里加一条 `Exploring + Emote → EmoteGestureRequested`，不碰任何仪式脚本。
**别做**：Router **只解析 + 发意图，不执行玩法**（执行在 Step 3）。

---

## Step 3 · CommandExecutor：唯一权威变更口（联机入场券·上半）

**为什么**：netcode 要求"状态变更有唯一收口、输入走命令"。现在 `PlayerInteraction.Update:161-204`
一堆 `if` 各自就地调用，是最难联网的形态。

**怎么改**（新建 `CommandExecutor`，注册进 `GameInstanceSubsystem`）：
- `Subscribe` 所有意图事件，作为**唯一**执行方调用领域逻辑：
  ```csharp
  bus.Subscribe<TendCampfireRequested>(OnTendCampfire);
  // 本地：直接执行；将来联机：改成发命令给服务器，玩法代码不动
  ```
- 所有"改共享状态"的调用（篝火添火 / 取火、余火增减、大树贡献）**只能从这里进**；
- `PlayerInteraction` 不再直接调 `NearestCampfire.TryTend(...)`——只负责探测目标 + 上报，执行归 executor。

**完成标准**：全项目对共享状态的写操作，调用点收敛到 executor 一处。
**为什么关键**：联机时只需把 executor 从"本地执行"换成"发命令给服务器 → 服务器裁决 → 广播领域事件"，玩法代码一行不动。

---

## Step 4 · 权威最小状态抽进 POCO + 注册进 Subsystem（联机入场券·下半）

**为什么**：服务器侧要能跑玩法逻辑、状态要能序列化。逻辑绑死在 MonoBehaviour + `Time.deltaTime` 里做不到。

**怎么改**（已有 `MarshmallowRoastSession`、`FishingResult` 做参考，但不做全项目一次性迁移）：
- 抽 `CampfireState`（warmth / level / totalContribution + `Tend / Decay / Withdraw` 纯逻辑）；
  `Campfire.cs` 变成"持有 CampfireState + 订阅领域事件驱动视觉 / 存档"的薄壳；
- 抽 `EmberEconomy`、`CampfireRegistry` 等领域服务，**注册进 `GameInstanceSubsystem`，按接口取**；
- 先抽中心广场会共享的玩家模式、中心篝火和共享仪式状态；钓鱼等纯本地玩法仅在确定共享时再抽；
- 领域状态一变，`Publish` 领域事件（`CampfireStateChanged` 等），视觉 / 音效 / UI / 存档订阅；
- POCO 不引用 `UnityEngine`（纯数据类型除外），`deltaTime` 作参数传入。

**完成标准**：`CampfireState` 与中心广场共享状态能在纯 C# 单元测试里运行，不需要 Unity 视觉组件。
**别做**：视觉 / 音效 / prefab 引用留在 MonoBehaviour，别塞进 POCO。

---

## Step 5 · 清理 + 内容扩展能力 Review

**怎么改**：
- `PlayerInteraction.Update` 两条并行分发**合成一条**：移动端动词按钮改为经 Router 发意图，不再各自重判目标；
- **删掉 `UpgradeCampfirePressedThisFrame` 那段漂移语义**（`:197-204`）；
- `MarshmallowInteraction`、`FishingInteraction` **不再轮询 `Emote / Interact`**，改为 `Subscribe`
  `RitualPrimaryRequested / RitualSecondaryRequested`；
- **吉他验收**：新增 `GuitarRitual`、吉他状态／表现和动画 Cue，但不修改 HUD、移动 Router、`RestInteraction`、已有仪式或网络层的玩法类型分支；
- **表情轮盘验收**：轮盘只提交 `EmoteRequested(emoteId)`；新增表情只增加定义、图标和动画 Cue；
- **动画验收**：本地意图与远端同步状态都驱动 `PlayerRitualAnimationController` 的同一语义 Cue／State，网络层不知道 Animator 参数。

**Review 通过标准**：新增一个表情或仪式时，改动集中在新内容、注册数据和表现资源；若必须修改核心 Router、HUD、CommandExecutor 或网络消息类型分支，则返回 Step 2／4 继续收口。

---

## Step 6 · 联机测试前置（与重构并行搭）

- 早点把**多实例跑起来**：ParrelSync，或"编辑器 + 一个 build"同跑——**实时联机无法一个人验证**。
- netcode 选型以 `FirePlay_联机接入评估.md` 为准（倾向 NGO + Relay 或 Fishnet，锁在中心火堆单实例）。

---

## 优先级与依赖

```
Step 1（输入进 Router）
   └─> Step 2（Router 解析 + 发意图·单一主人）── 解锁表情 / 赠与 / 吉他
          └─> Step 3（CommandExecutor 唯一变更口）── 联机上半
Step 4（领域 POCO 化 + 注册 Subsystem）── 联机下半，可与 2/3 并行
Step 5（清理 + 表情／吉他／动画 Review）── 联网前内容扩展门槛
Step 6（多实例 / 选型）── 并行准备
```

- **最小可停点**：做完 1 + 2 后，离散输入已有唯一语义主人。
- **联机资格线**：Step 1–5 全部完成，且表情轮盘、吉他仪式和动画 Review 通过。**在此之前不要接入正式 netcode。**

---

## 附：不在这条链上的东西

- **踩地板特效 / 脚步音**：纯本地表现，走**动画事件**（落脚帧打 Animation Event → `FootstepReceiver`
  向下 raycast 查表面 → 播池化 SFX / VFX），零 Update 轮询，联机安全。想先插入随时可以。
- **美术 / 氛围 / URP / 雾 / 灯光**：另一条线，与本重构无关。
